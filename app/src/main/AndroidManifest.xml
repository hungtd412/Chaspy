<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.chaspy">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chaspy"
        tools:targetApi="31">

        <!-- SignUpActivity -->
        <activity
            android:name=".ui.view.SignUpActivity"
            android:exported="true">

        </activity>

        <!-- SignInActivity (launcher) -->
        <activity
            android:name=".ui.view.SignInActivity"
            android:exported="true">

        </activity>

        <!-- MainActivity (nếu có dùng sau login) -->
        <activity
            android:name=".ui.view.ConversationsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
